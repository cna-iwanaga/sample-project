module.exports = {
  root: true,
  env: {
    browser: true,
    node: true
  },
  parser: 'vue-eslint-parser',
  parserOptions: {
    parser: '@typescript-eslint/parser'
  },
  extends: [
    'plugin:@typescript-eslint/recommended', // TypeScript Recommended Rule.
    'plugin:vue/essential', // Vue.js Style Guide Priority A.
    'plugin:prettier/recommended', // Prettier Recommended Rule.
    'prettier/@typescript-eslint' // Support @typescript-eslint/eslint-plugin.
  ],
  rules: {
    'no-console': process.env.NODE_ENV === 'production' ? 'error' : 'off',
    'no-debugger': process.env.NODE_ENV === 'production' ? 'error' : 'off'
  },
  overrides: [
    {
      files: ['**/__tests__/*.{j,t}s?(x)', '**/tests/unit/**/*.spec.{j,t}s?(x)'],
      env: {
        jest: true
      },
    }
  ],
};
